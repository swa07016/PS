#include <iostream>

int main() {
    // long long 자료형을 사용하여 큰 수를 처리할 수 있도록 합니다.
    long long A, B, C;

    // A, B, C 값을 입력받습니다.
    std::cin >> A >> B >> C;

    // A, B, C의 합을 계산하고 출력합니다.
    std::cout << A + B + C << std::endl;

    return 0;
}
