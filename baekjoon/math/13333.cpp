#include <iostream>
#include <algorithm>

using namespace std;
int n;
int cnt[1010];

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    cin >> n;
    for(int i=0; i<n; i++)
        cin >> cnt[i];


    for(int i=0; i<=10000; i++) {
        
    }
    
    return 0;
}